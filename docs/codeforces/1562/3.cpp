#include <bits/stdc++.h>
#define ll long long int

using namespace std;

int main(){
    ll t;
    cin >> t;
    while(t--){
        int n;
        string s;
        vector<int> d(n);
        for (int i = 0; i < s.length(); i++) {
            d[i] = s[i] - '0';
        }
        int l1, r1, l2, r2;
        
    }
    return 0;
}