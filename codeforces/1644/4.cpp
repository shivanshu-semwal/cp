#include <bits/stdc++.h>
using namespace std;

int MOD = 998244353;

void solve() {
    int n, m, k, q;
    cin >> n >> m >> k >> q;
    vector<pair<int, int>> que;
    while (q--) {
        int t1, t2;
        cin >> t1 >> t2;
        que.push_back({t1, t2});
    }


}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}